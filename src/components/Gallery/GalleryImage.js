import React from 'react';
import styled from "styled-components";

const GalleryImage = ({img}) => {
    return (
        <ImageWrapper>
            <img src={} alt="Nasz produkt"/>
        </ImageWrapper>
    );
};

const ImageWrapper = styled.div`

`;

export default GalleryImage;